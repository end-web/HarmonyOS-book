# HarmonyOS 代码混淆规则

# ─── 启用混淆 ───
-enable-property-obfuscation
-enable-toplevel-obfuscation
-enable-filename-obfuscation
-enable-export-obfuscation

# 名称缓存（增量构建）
-enable-name-cache
-print-namecache obfuscation-cache.json

# ─── 保留 JSON 序列化/反序列化的属性名 ───
# 这些属性来自外部 JSON（Legado 书源格式）或本地持久化，
# 通过 JSON.parse / JSON.stringify 访问，混淆器无法静态追踪，必须显式保留。
-keep-property-name
  # Huawei account persisted identity
  unionID
  openID
  loginAt
  nickName
  avatarFile
  # System / personal cloud backup snapshot fields
  exportedAt
  preferences
  stats
  ruleSources
  favorites
  history
  lastPlayed
  progresses
  skipConfigs
  descendingBookIds
  colorMode
  materialStyle
  accentColor
  defaultTab
  autoPlayOnLaunch
  audioInterruptMode
  audioAutoResume
  homeGridColumns
  preloadChapters
  # Reading backup and persisted reading/speech positions
  reading
  positions
  settings
  speechPositions
  speechSpeed
  speechPerson
  speechLanguage
  speechFollow
  resourceIndex
  domPos
  charOffset
  pageOffset
  fontSize
  lineHeight
  fontWeight
  themeId
  pageTurnMode
  showReadingStatus
  alwaysShowStatusBar
  followSystemTheme
  marginLeft
  marginRight
  marginTop
  marginBottom
  screenOnMinutes
  bookId
  chapterId
  chapterTitle
  progressMs
  positionMs
  chapterIndex
  durationMs
  updatedAt
  # Legacy playback JSON aliases must remain readable during backup migration
  h15
  q11
  c10
  h26
  m56
  l50
  n49
  x47
  u47
  speed
  listenSeconds
  days
  day
  validationStatus
  validationMessage
  variable
  # BookSource
  bookSourceUrl
  bookSourceName
  bookSourceType
  bookSourceGroup
  bookSourceComment
  enabled
  enabledExplore
  enabledCookieJar
  searchUrl
  exploreUrl
  header
  loginUrl
  loginUi
  loginCheckJs
  bookUrlPattern
  variableComment
  jsLib
  customButton
  eventListener
  exploreScreen
  coverDecodeJs
  respondTime
  customOrder
  isLocked
  isPinned
  weight
  concurrentRate
  ruleSearch
  ruleToc
  ruleContent
  ruleBookInfo
  ruleExplore
  ruleReview
  lastUpdateTime
  subscriptionUrl
  # SearchRule / ExploreRule
  bookList
  nextPageUrl
  coverUrl
  bookUrl
  intro
  kind
  lastChapter
  wordCount
  checkKeyWord
  updateTime
  # TocRule
  chapterList
  chapterName
  chapterUrl
  nextTocUrl
  isVip
  isPay
  isVolume
  duration
  preUpdateJs
  formatJs
  # ContentRule
  content
  audioUrl
  nextContentUrl
  replaceRegex
  sourceRegex
  webJs
  # BookInfoRule
  init
  canReName
  downloadUrls
  tocUrl
  # ReviewRule
  reviewList
  avatar
  reviewUrl
  # Book
  id
  contentType
  title
  author
  narrator
  cover
  category
  totalDuration
  chapterCount
  rating
  tags
  chapters
  sourceUrl
  variables
  skipIntro
  skipOutro
  # Chapter
  index
  source
  originSourceValue
  # AudioSource
  type
  value
  # SearchResult
  sourceName
  # HomeBlockSection
  books
  moreUrl
  layout
  # SourceSubscription
  count
  lastUpdate
  # TocItem
  url
  name
  # Talebook NAS API JSON fields
  err
  msg
  sys
  version
  total
  book
  manifest
  edition
  files
  format
  href
  comments
  img
  book_id
  chapter_count
  number
  duration_ms
  audio_url

# ─── 保留文件名（避免资源路径问题） ───
# 注：-enable-filename-obfuscation 已启用，但 resources/rawfile 等资源引用
# 在代码中以字符串出现，混淆器可处理。如遇问题可注释掉 -enable-filename-obfuscation。
